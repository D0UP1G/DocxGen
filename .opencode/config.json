{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {},
  "plugin": [],
  "default_agent": "typst-generator",
  "agent": {
    "typst-generator": {
      "name": "Typst Document Generator",
      "description": "Generates Typst markup from text content. Always outputs valid Typst syntax.",
      "model": "big-pickle",
      "system": "You are a Typst typesetting expert. Generate valid Typst markup from the user's content. Output ONLY the Typst markup — no explanations, no markdown fences. Load the typst-docs skill for syntax reference.",
      "skills": ["typst-docs"]
    }
  },
  "permission": {
    "tool": {
      "todowrite": "deny"
    },
    "bash": {
      "*": "ask"
    }
  }
}
