{
  "name": "@docxgen/backend",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=../../.env --watch src/server.js",
    "start": "node src/server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "better-sqlite3": "^12",
    "cookie-parser": "^1",
    "docx": "^9",
    "express": "^5",
    "pino": "^9",
    "pino-http": "^10",
    "zod": "^4"
  },
  "devDependencies": {
    "jszip": "^3",
    "supertest": "^7",
    "vitest": "^3"
  }
}
