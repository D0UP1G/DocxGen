{
  "name": "docxgen",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names doc,audio,max,vk,local,web --prefix-colors blue,cyan,yellow,magenta,green,red \"npm --prefix packages/backend run start:doc-service\" \"npm --prefix packages/backend run start:audio-service\" \"npm --prefix packages/backend run start:max-bot\" \"npm --prefix packages/backend run start:vk-bot\" \"npm --prefix packages/backend run start:local-bot\" \"cd packages/frontend && npm run dev\"",
    "start": "concurrently --names doc,audio --prefix-colors blue,cyan \"node --env-file-if-exists=.env packages/backend/src/document-service.js\" \"node --env-file-if-exists=.env packages/backend/src/audio-service.js\"",
    "start:audio": "npm --prefix packages/backend run start:audio-service",
    "test": "pnpm --filter @docxgen/backend test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "eval": "pnpm --filter @docxgen/backend eval",
    "opencode:check": "pnpm --filter @docxgen/backend opencode:check",
    "max:subscribe": "pnpm --filter @docxgen/backend max:subscribe"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild"
    ]
  }
}
